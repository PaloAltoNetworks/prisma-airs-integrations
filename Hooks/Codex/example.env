# Claude Code x Prisma AIRS Hooks — environment variables
# Set these in your shell profile (~/.zshrc, ~/.bashrc) on macOS/Linux, or via
# System Settings > Environment Variables (or `setx`) on Windows.

# --- Required --------------------------------------------------------------

# Prisma AIRS API key.
#   Strata Cloud Manager > AI Runtime Security > API Intercept
export PRISMA_AIRS_API_KEY="your-airs-api-key-here"

# Security profile — set ONE of these (profile_id wins if both are set).
#   Strata Cloud Manager > AI Runtime Security > Security Profiles
export PRISMA_AIRS_PROFILE_NAME="your-security-profile-name"
# export PRISMA_AIRS_PROFILE_ID="00000000-0000-0000-0000-000000000000"

# --- Optional --------------------------------------------------------------

# Regional API endpoint (base URL only — the scan path is appended for you).
# Default: US.
# export PRISMA_AIRS_URL="https://service-de.api.aisecurity.paloaltonetworks.com"  # EU (Germany)
# export PRISMA_AIRS_URL="https://service-in.api.aisecurity.paloaltonetworks.com"  # India
# export PRISMA_AIRS_URL="https://service-sg.api.aisecurity.paloaltonetworks.com"  # Singapore

# Behaviour on a *runtime* AIRS error (network down, timeout, 5xx):
#   open   = allow the action, log a warning (default — never bricks the agent)
#   closed = block input-side actions (strict; no unverified content proceeds)
# A MISSING key/profile is always fail-closed on the input side regardless.
# export AIRS_FAIL_MODE="open"

# Per-scan timeout in milliseconds (default 10000) and retry count (default 1).
# export AIRS_TIMEOUT_MS="10000"
# export AIRS_RETRIES="1"

# Chunking of oversized content. Content longer than AIRS_MAX_CONTENT_CHARS is
# scanned in overlapping chunks of that size, up to AIRS_MAX_CHUNKS chunks, so
# an injection hidden past the cap is still caught (nothing is silently dropped).
# Content beyond the budget (chars x chunks) is treated per AIRS_FAIL_MODE and
# logged — never silently allowed. Defaults: 20000 chars x 6 chunks = 120000.
# export AIRS_MAX_CONTENT_CHARS="20000"
# export AIRS_MAX_CHUNKS="6"

# Log file location (default: .claude/hooks/prisma-airs.log, relative to cwd).
# export SECURITY_LOG_PATH="/absolute/path/to/prisma-airs.log"

# Code-aware scanning (ON by default). Also sends content as code_prompt /
# code_response so AIRS runs multi-language Malicious Code Detection on tool
# input, tool output, and the model's answer (returns malicious_code). Requires
# an AIRS profile with Malicious Code Detection enabled (harmless otherwise —
# the extra fields are ignored). Set to "0" to disable.
# export AIRS_CODE_AWARE="0"

# DLP mask-in-place (opt-in). When "1", an allowed tool call/result that AIRS
# would DLP-mask is REDACTED in place (sensitive data replaced) instead of
# passing raw to the model — via updatedInput (tool input) / updatedToolOutput
# (tool output). Never overrides a block; adds one scan per allowed tool surface.
# Requires an AIRS security profile with basic DLP set to the Block action
# (masking is a Block-action feature). Prompts and the model's answer can't be
# rewritten by Claude Code, so those stay block-or-allow.
# export AIRS_ENABLE_MASKING="1"

# Suffix appended to the AIRS app_name for this instance. The base name is the
# active agent (Claude Code / Codex CLI / Cursor / Cline / Windsurf Cascade), so
# a suffix of "team-a" reports e.g. "Codex CLI-team-a". CLAUDE_CODE_APP_SUFFIX is
# still honoured for back-compat.
# export AIRS_APP_SUFFIX="team-a"

# Verbose debug logging to the log file and stderr.
# export AIRS_DEBUG="1"
